namespace Solution {
    public enum eColorType {
        Red,
        Orange,
        Green,
    }

    public enum eGameLevel {
        Easy,
        Hard
    }

    public enum eTimerState {
        Idle,
        Activate,
        Deactivate
    }

    public enum ePanelType {
        Result,
    }
}